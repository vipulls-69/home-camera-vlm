## To run the server

```
cd backend
source venv/bin/activate
pip install opencv-python ultralytics numpy pillow groq fastapi uvicorn
python main.py
```

Make sure to setup config.py before running the project
